apiVersion: v1
kind: Service
metadata:
  name: platformlz-demo
  labels:
    app: platformlz-demo
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
spec:
  type: LoadBalancer
  loadBalancerIP: 10.20.2.10
  selector:
    app: platformlz-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
